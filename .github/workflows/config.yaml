name: Deployment Setup

on: push

jobs: 
  deploy: 
    runs-on: ubuntu-latest
    environment: test 

    steps: 
      - name: print single line
        run: echo ${{secrets.msg}}
        
      - name: Check secrets
        run: | 
          if [ "${{secrets.msg}}" == "vinoth"]; then
            echo "Secret Matched"
          else
            echo "Secrets not Exists"  
          fi 
